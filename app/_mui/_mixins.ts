import { Mixins } from "@mui/material/styles/createMixins";

const _mixins: Mixins = {
  toolbar: {
    minHeight: 56,
  },
  customMixin: {
    padding: "16px",
    backgroundColor: "#eee",
  },
};

export default _mixins;
